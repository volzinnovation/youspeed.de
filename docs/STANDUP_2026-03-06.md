# Standup 2026-03-06

## Completed today

- tightened the iPhone matcher to use continuity context instead of a single previous way only:
  - previous matched way
  - short recent-way history
  - recent street-ref history
  - recent tunnel-candidate memory
  - recent GPS-signal-loss flag
- implemented same-ref transition promotion so route continuation can advance to the next segment instead of clinging to the previous segment too long
- added selective mini-HMM style route continuity scoring in the matcher for ambiguous local transitions
- integrated `way_links` into the consumer matcher transition logic so topology can bias route continuation on-device
- added tunnel gating in the matcher:
  - tunnel-tagged ways are not selected unless continuity supports them
  - after signal loss, a tunnel candidate can be re-entered only if it was already seen in the nearby candidate set
- added simulator regression coverage for the Karlsruhe route case:
  - expected sequence `52869774 -> 1316349759 -> 206811644 -> 1220097540`
  - explicit guard that candidate `16657591` is nearby but must not win
- added a second regression route for tunnel-adjacent false matches:
  - `209270482 -> 1251752493 -> 1252070523 -> 1036502006 -> 1251752490 -> 209270485 -> 1037006038`
  - nearby tunnel candidate stays available but must not win
- implemented two Karlsruhe benchmark bundle variants:
  - `v3_A`: `max-geom-points = 16`
  - `v3_B`: `way_links`
- automated benchmark execution in the benchmark app for the Karlsruhe route scenarios, kept out of consumer runtime code
- assembled Karlsruhe seed PBF parts locally, applied a real `.osc` diff window, rebuilt runtime DBs, and measured exact delta-patch cost for `way_links`
- optimized `way_links` SQL patch generation:
  - removed slow cast-heavy deletes
  - switched to indexed integer batched deletes
  - reduced patch apply time from seconds to low hundreds of milliseconds
- fixed a production startup issue where the app target did not package the seed database
  - added `/Users/raphaelvolz/Github/youspeed.de/iphone/SpeedConsumerApp/karlsruhe-regbez_speeds.sqlite` as an app resource
  - rebuilt and redeployed successfully
- built and deployed the current `SpeedConsumer` app to the paired iPhone outside the sandbox
- updated the technical report so the implementation section now matches:
  - continuity-aware matching
  - same-ref transition logic
  - tunnel gating with GPS-loss recovery
  - current bundle-update artifacts

## Resulting status

- simulator regression tests for the matcher are green
- Karlsruhe route benchmark infrastructure is in place and reproducible
- `v3_B` (`way_links`) is currently the better runtime choice on query time:
  - `v3_A` mean: about `9.88 ms/fix`
  - `v3_B` mean: about `9.36 ms/fix`
- `way_links` delta maintenance overhead is now measurable and bounded on the Karlsruhe 3-day window:
  - patch size about `+23.6%`
  - patch apply time about `+79 ms`
  - apply-time ratio about `1.93x`
- the bundled seed database now loads correctly on the iPhone
- device build and install succeeded
- technical report text is aligned with the current app logic in the matching and tunnel sections

## Open items for next sessions

- validate the `way_links` design on longer real-drive traces, not only the two Karlsruhe regression routes
- decide whether the current `way_links` representation is sufficient, or whether bundle generation should move from endpoint-based links to true shared-node adjacency
- benchmark end-to-end route stability on device with logged real drives, not only per-fix lookup timings
- reassess whether tunnel exit needs explicit hysteresis after GPS reacquisition in difficult urban/tunnel transitions
- evaluate whether same-ref continuity should be weighted by route class or confidence, especially where refs are re-used across nearby parallel roads
- decide whether to adopt `v3_B` as the default consumer bundle format, given:
  - query-time win
  - larger bundle size
  - higher delta-maintenance cost
- extend paper and tech-report evaluation with the measured `way_links` update/query tradeoff if we want the research artifacts to match the shipped implementation
- patch the iPhone deploy/test scripts so they handle paired devices and token injection more robustly
- keep the technical report and app documentation synchronized after the next runtime-data changes
